[project]
name = "strava-shoe-comparison"
version = "0.1.0"
description = "Compare running shoes by analyzing Strava activities"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
strava-shoe-comparison = "main:main"
